spring:
    application:
        name: config-client

    cloud:
        config:
            uri: http://localhost:8001

    profiles:
        active: northamerica

    rabbitmq:
        host: 10.13.120.96
        port: 5671

server:
    port: 8002